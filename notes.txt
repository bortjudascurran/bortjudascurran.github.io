CSS
width sets the width of stuff, percentages will do the width 
compared to the default width, so for example, the default width
of a paragraph is 100% of the display screen

font-size using rem makes it change relative to the default 
element size selected by the browser

AI image generator: Automatic 1111

{for (let n = 0; n <= text.length; n++)
                {
                    let letter = text.charAt(n)
                    console.log(letter)
                    setTimeout(
                        function()
                        {
                            letterAdd(letter);
                        }
                        , 50*n);
                }
            }, 1000); //Delayed by 1 second
        }